const {people}  = require('./logger') // old syntax
// import { }

// console.log(logg);
// console.log(logg.toString());
console.log(people);

const oss = require('os');
console.log(oss.platform(),oss.homedir());
